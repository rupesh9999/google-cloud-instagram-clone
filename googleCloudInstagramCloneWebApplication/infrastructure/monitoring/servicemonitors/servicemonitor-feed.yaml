---
# =============================================================================
# ServiceMonitor - Feed Service
# Prometheus scraping configuration for feed aggregation service
# =============================================================================
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: feed-service-monitor
  namespace: monitoring
  labels:
    app: feed-service
    team: instagram-clone
    release: instagram-monitoring
spec:
  namespaceSelector:
    matchNames:
      - instagram-clone
  selector:
    matchLabels:
      app: feed-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s
      scheme: http
      honorLabels: true
      metricRelabelings:
        - sourceLabels: [__name__]
          regex: 'jvm_.*|http_.*|spring_.*|hikaricp_.*|process_.*|system_.*|lettuce_.*'
          action: keep
        - sourceLabels: [__name__]
          targetLabel: service
          replacement: feed-service
      relabelings:
        - sourceLabels: [__meta_kubernetes_pod_name]
          targetLabel: pod
        - sourceLabels: [__meta_kubernetes_namespace]
          targetLabel: namespace
        - sourceLabels: [__meta_kubernetes_pod_node_name]
          targetLabel: node
